// 1. Number Type
let num1 = 27;
let num2 = 1.5;
let num3 = -20;

let inf = Infinity; // 양의 무한대
let mInf = -Infinity; // 음의 무한대
let nan = NaN; // 숫자로 연산이 실패했을 경우 표기 (Not a Number)

// 2. String Type
let myName = "이정환";
let myLocation = "목동"
let introduce = myName + myLocation;
let introduceText = `${myName}은 ${myLocation}에 거주합니다.` // 템플릿 리터럴 문법
// console.log(introduceText);

// 3. Boolean Type (참/거짓)
isSwitchOn = true;
isEmpty = false;

// 4. Null Type (해당 변수에 반환값이 없음을 명시할 때 직접 할당해줘야 하는 타입)
let empty = null;
// console.log(empty);

// 5. Undefined Type (변수값이 지정되지 않았을 때 자동 할당되는 타입)
let none;
// console.log(none);